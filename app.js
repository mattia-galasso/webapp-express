const express = require("express");
const app = express();

const port = 3000;
const appURL = `http://localhost:${port}`;

// IMPORTS

// MIDDLEWARES

// ROUTERS

// ERROR HANDLERS

// LISTENER
app.listen(port, () => {
  console.log(`Server listening on ${port}`);
});
